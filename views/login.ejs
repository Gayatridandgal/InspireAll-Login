<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - InspireAll</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <nav>
        <div class="nav-logo">
            <img src="/logo.png" alt="InspireAll Logo" class="logo">
            <span class="site-title">InspireAll</span>
        </div>
        <div class="nav-links">
            <a href="/"><i class="fas fa-home"></i> Home</a>
            <a href="/business-info"><i class="fas fa-briefcase"></i> Business Info</a>
            <a href="/post"><i class="fas fa-edit"></i> Post</a>
            <a href="/schemes"><i class="fas fa-seedling"></i> Schemes</a>
            <a href="/login" class="btn-signin"><i class="fas fa-sign-in-alt"></i> Sign in</a>
            <a href="/signup" class="btn-signup"><i class="fas fa-user-plus"></i> Sign up</a>
        </div>
    </nav>

    <div class="container">
        <div class="header">
            <img src="/logo.png" alt="InspireAll Logo" class="logo">
            <h1 class="site-title">InspireAll</h1>
        </div>
        <h2>Sign In</h2>
        
        <% if (message && message.length) { %>
            <div class="message"><%= message %></div>
        <% } %>
        
        <form action="/login" method="POST">
            <div class="input-group">
                <i class="fas fa-envelope"></i>
                <input type="email" name="email" placeholder="Email" required />
            </div>
            <div class="input-group">
                <i class="fas fa-lock"></i>
                <input type="password" name="password" placeholder="Password" required />
            </div>
            <div style="text-align: right; margin-top: -0.5rem;">
                <a href="#" style="font-size: 0.8rem; color: var(--text-light);">Forgot password?</a>
            </div>
            <button type="submit"><i class="fas fa-sign-in-alt"></i> Sign In</button>
        </form>
        
        <div class="links">
            <p>Don't have an account? <a href="/signup">Sign up</a></p>
        </div>
    </div>
</body>
</html>